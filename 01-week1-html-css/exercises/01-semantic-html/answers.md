# Answers
The <div class="top"> has been replaced with a <header> element, which is more appropriate for containing introductory content or navigational links.
The menu items are now wrapped in an unordered list (<ul>), with each item as a list item (<li>). This structure is more semantic.The main content is now wrapped in a <main> tag, which signifies the primary content of the document.
Each section of content is wrapped in <section> tags, which clearly delineates different parts of the page .The titles have been changed to appropriate heading tags <h1> for the main title and <h2> for section titles.The contact form is now wrapped in a <form> tag, which is essential for any interactive form. Each input field has a corresponding <label> for better accessibility, allowing screen readers to announce the purpose of each input.

Write 5–8 sentences: what you changed and why.
